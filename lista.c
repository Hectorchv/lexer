#include<stdio.h>
#include<stdlib.h>
#include<assert.h>
#include<string.h>

#include"lista.h"

add_t_numerica(t_numerica *tabla, float valor)
{
    t_numerica *tmp = malloc(sizeof(t_numerica));

    assert(tmp != NULL);

    
}

add_t_cadena(t_cadena *tabla, char *valor);
add_t_ident(t_ident *tabla, char *nombre, int tipo);